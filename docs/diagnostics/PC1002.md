# PC1002: TypeScript consumer compilation failed

A generated strict TypeScript consumer produced one or more compiler errors
while importing a declared package entrypoint.

The invoking project's real TypeScript compiler is used through its structured
API. The diagnostic applies only when the package declares types for the tested
subpath. Unsupported or unavailable compilers are reported as `not-evaluated`.
