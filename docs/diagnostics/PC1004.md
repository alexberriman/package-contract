# PC1004: Package executable failed

A declared package executable returned a nonzero exit code when invoked with an
explicit consumer action.

Executable probes are opt-in because a zero-configuration check cannot infer
safe arguments or expected behavior. npm installs the tarball with lifecycle
scripts disabled, then the declared shim runs directly with a bounded
environment, output budget, and timeout.
