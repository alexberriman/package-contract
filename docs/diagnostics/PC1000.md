# PC1000: Consumer installation failed

The packed tarball could not be installed into an isolated npm consumer.

The diagnostic includes the exact normalized npm command and a bounded excerpt
of npm output. Installation runs with lifecycle scripts disabled, an isolated
configuration, and no audit or funding requests.

In strict offline mode, a recognized cache miss is reported as `not-evaluated`
instead of PC1000.
